const express = require("express")
const router = express.Router()


// Provider: total earnings, today, month
router.get("/earnings", async (req, res)=>{
    try {
        
    } catch (error) {
        
    }
})

// Provider: recent transactions
router.get("/recent-earning", async (req, res)=>{
    try {
        
    } catch (error) {
        
    }
})



module.exports = router